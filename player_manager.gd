extends Node

var player: Player

#func _ready() -> void:
	#player = get_tree().get_first_node_in_group("Player")
#
#func reparent_player(new_parent):
	#call_deferred("Node2D.reparent()", new_parent)
	#player.reparent(new_parent)
